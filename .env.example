# =============================================================================
# Server Configuration
# =============================================================================

# PostgreSQL connection (omit POSTGRES_HOST_PORT to use in-memory SQLite)
POSTGRES_USER=verve
POSTGRES_PASSWORD=verve
POSTGRES_HOST_PORT=postgres:5432
POSTGRES_DATABASE=verve

# 32-byte hex key for encrypting secrets (required for GitHub token storage)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=dd78085780adfd57c3f7023e480349cdb87d8e624f3ec97ca34ff7bded1bbcc5

# Enable embedded UI (set to "true" to serve the frontend from the server)
UI=true

# =============================================================================
# Worker Configuration
# =============================================================================

# API server URL the worker connects to
API_URL=http://server:7400

# Claude authentication (one of these is required unless DRY_RUN=true)
ANTHROPIC_API_KEY=
CLAUDE_CODE_OAUTH_TOKEN=

# Claude model to use (haiku, sonnet, opus)
CLAUDE_MODEL=haiku

# Docker image for agent containers
AGENT_IMAGE=verve-agent:latest

# Maximum concurrent tasks the worker will run
MAX_CONCURRENT_TASKS=3

# Skip Claude API calls for testing (creates dummy changes)
DRY_RUN=false
